git init
git status
git add . (add arquivos)
git commit -m "razão"
git push (enviar dados ao github)
	git remote (nome) url
	git push --set-upstream (nome que aparecer)


git reflog (ver versãos)
git reset -- hard (code)
git branch (ver)
git branch nome (add)
git checkout (nome da branch for switch)
git merge staging